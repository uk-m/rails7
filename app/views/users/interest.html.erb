<div class="row justify-content-center">
  <div class="col-4">
    <h1>ユーザー詳細</h1>
    <% if @user.image.attached? %>
      <%= image_tag @user.image.variant(resize:'200x200') %>
    <% end %>
    <%= @user.name %>
    <section>
      <%= render 'shared/stats' %>
    </section>
    <%= render 'follow_form' if logged_in? %>
    <%= link_to '保存している質問', interest_user_path(@user) %>
    <%= link_to 'いいねしている回答', like_user_path(@user) %>
  </div>
  <div class="col-4">
    <h3>保存している質問</h3>
    <%= render @questions %>
  </div>
</div>